@*@model IEnumerable<Cigarette.Enterprise.ViewModels.ViewModels.Advertisement.AdvertisementDetailsViewModel>
    @using Cigarette.Enterprise.ViewModels.ViewModels.AdvertismentImage*@
@using Cigarette.Enterprise.ViewModels.ViewModels.CommercialAd;
@{
    Layout = null;
    var baseUrl = System.Configuration.ConfigurationManager.AppSettings["ApiDomain"] + "/DynamicResources/CommercialAdsImages/";
}

@if (Model != null)
{

    var language = Saudiceos.Enterprise.Web.helper.CookieHelper.IsArabic();
    @*<div class="col-md-6 ">
            <div id="carouselExampleIndicators" class="carousel slide row" data-ride="carousel">
                <div class="col-2">
                    <ol class="carousel-indicators productimgSelect">
                        @{
                            int qcount1 = 0;
                        }
                        @foreach (var item in (List<AdvertismentImageBM>)ViewBag.AdImages)
                        {
                            if (qcount1++ == 0)
                            {
                                <li data-target="#carouselExampleIndicators" data-slide-to="@qcount1" class="active"><img class="img-fluid img-ad" src="@(string.IsNullOrWhiteSpace(item.Url)?"~/Content/arabic/images/defaultimage.jpg":(baseUrl+item.Url))" name="@(string.IsNullOrWhiteSpace(item.Url)?"noimg":item.Url)"></li>
                            }
                            else
                            {
                                <li data-target="#carouselExampleIndicators" data-slide-to="@qcount1"><img class="img-fluid img-ad" src="@(string.IsNullOrWhiteSpace(item.Url)?"~/Content/arabic/images/defaultimage.jpg":(baseUrl+item.Url))" name="@(string.IsNullOrWhiteSpace(item.Url)?"noimg":item.Url)"></li>
                            }


                        }


                    </ol>
                </div>
                <div class="col-10">


                    <div class="carousel-inner">
                        @{
                            int qcount2 = 0;
                        }
                        @foreach (var item in (List<AdvertismentImageBM>)ViewBag.AdImages)
                        {
                            if (qcount2++ == 0)
                            {
                                <div class="carousel-item active">
                                    <img class="d-block w-100 img-ad" src="@(string.IsNullOrWhiteSpace(item.Url)?"~/Content/arabic/images/defaultimage.jpg":(baseUrl+item.Url))" name="@(string.IsNullOrWhiteSpace(item.Url)?"noimg":item.Url)" alt="First slide">
                                </div>
                            }
                            else
                            {
                                <div class="carousel-item">
                                    <img class="d-block w-100 img-ad" src="@(string.IsNullOrWhiteSpace(item.Url)?"~/Content/arabic/images/defaultimage.jpg":(baseUrl+item.Url))" name="@(string.IsNullOrWhiteSpace(item.Url)?"noimg":item.Url)" alt="Second slide">
                                </div>
                            }
                        }


                    </div>


                    <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                        <span class="sr-only">Previous</span>
                    </a>
                    <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                        <span class="carousel-control-next-icon" aria-hidden="true"></span>
                        <span class="sr-only">Next</span>
                    </a>
                </div>
            </div>
        </div>*@

    <div class="col-md-6 productContent">
        @*<h1>@Model.FirstOrDefault().AdvertisementTitle</h1>*@
        <div class="content">
            @Model.AdvertisementDescription

        </div>
        <div class="content">
            <div class="row">
                <div class="col-6">
                    <span class="productIcon">
                        <i class="far fa-clock"></i>
                    </span>
                    <span class="proDetails">
                        منذ @Model.
                    </span>
                </div>
                <div class="col-6">
                    <span class="productIcon">
                        <i class="far fa-eye"></i>
                    </span>
                    <span class="proDetails">
                        عدد المشاهدات : @Model.FirstOrDefault().Views
                    </span>
                </div>
            </div>
        </div>
        @*<div class="row">
                <div class="col-4 productPrice">
                    <h3 style="color: #55b655"> @(language ? Model.FirstOrDefault().PriceTextAr : Model.FirstOrDefault().PriceTextEn)</h3>
                </div>


                <div class="col-6  productCat">
                    <ul>


                        @foreach (var item in Model)
                        {
                            if (item.Advertisment_SpecificationAttributeCustomValue != null && item.Category_SpecificationAttributeName != null)
                            {
                                <li>@item.Category_SpecificationAttributeName : @item.Advertisment_SpecificationAttributeCustomValue </li>
                            }
                            if (item.Category_SpecificationAttributeName != null && item.SpecificationAttributeOptionName != null)
                            {
                                <li>@item.Category_SpecificationAttributeName : @item.SpecificationAttributeOptionName</li>
                            }
                        }


                    </ul>
                </div>


            </div>*@
    </div>


}



<script src="~/Scripts/js/ltr/jQuery_3.4.1.js"></script>

<script>
    $(document).ready(function () {

        @*$('.img-ad').each(function () {

            var imge = $(this);
            var ImgName = $(imge).attr('name');
            if (ImgName != "noimg") {
                var model = { ImageName: ImgName };

                var settings = {
                    "async": true,
                    "crossDomain": true,
                    "url": "@System.Configuration.ConfigurationManager.AppSettings["ApiDomain"]"+"/Api/ImageUpload/GetAdImage",
                    "method": "POST",
                    "headers": {
                        "Content-Type": "application/json",
                        "cache-control": "no-cache",
                    },
                    "processData": false,
                    "data": "{\"ImageName\":\"" + ImgName + "\"}"
                }

                $.ajax(settings).done(function (response) {
                    $(imge).attr('src', response);
                });
            }
        });*@
    });


</script>
